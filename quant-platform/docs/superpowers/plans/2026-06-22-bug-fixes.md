# 8-Bug 修复实施 Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 CODEX 静态扫描报告的 8 个数据/模拟盘 bug,严格遵循"零新问题 + 业务连续性"硬约束

**Architecture:** 8 个原子 commit,按 4 个域分组(A 数据/B Agent/C 模拟盘/D 回归),C 域严格 C1→C2→C3→C4 顺序。每个 commit 含修复 + 测试 + 5 步强制验证。

**Tech Stack:** Python 3.x, FastAPI, DuckDB, Pandas, LangGraph, LangChain

**Spec:** [docs/superpowers/specs/2026-06-22-bug-fixes-design.md](../specs/2026-06-22-bug-fixes-design.md)

---

## 硬约束(每个 task 必读)

> 修任何 bug 前,**必须问"会不会引入新问题"**。如果会,放弃这个改法,找不会引发新问题的方案。
> 修复期间用户**暂停止损/实盘/模拟盘自动交易**,只保留查询。
> 每个 commit 完成后,跑 5 步强制验证(见 Task 1 的 Step 7)。

---

## 文件结构(本次修复涉及的文件)

### 修改文件

| 文件 | 改的原因 | 涉及 Task |
|---|---|---|
| `database/duckdb_manager.py` | 加 `update_stock_list` alias | Task 1 |
| `app/data_manager/engine.py` | 修 #2 裸 except、#7 amount | Task 1, Task 2 |
| `config/app_setting.json` | 删"风险"死配置 | Task 3 |
| `app/agents/committee.py` | 修 #10 LLM key 校验 + 模型名 | Task 4 |
| `app/agents/strategy_coder.py` | 修 #10 同样的问题(扩展) | Task 4 |
| `app/sim_trader/engine.py` | 修 #11 / #8 / #9 | Task 5, Task 7, Task 8 |
| `app/sim_trader/intraday_monitor.py` | 修 #11 / #13 / #9 | Task 5, Task 6, Task 8 |

### 新建文件

| 文件 | 作用 | 涉及 Task |
|---|---|---|
| `scripts/test_fix_02.py` | #2 回归测试 | Task 1 |
| `scripts/test_fix_07.py` | #7 回归测试 | Task 2 |
| `scripts/test_fix_16.py` | #16 回归测试 | Task 3 |
| `scripts/test_fix_10.py` | #10 回归测试 | Task 4 |
| `scripts/test_fix_11.py` | #11 回归测试 | Task 5 |
| `scripts/test_fix_13.py` | #13 回归测试 | Task 6 |
| `scripts/test_fix_08.py` | #8 回归测试 | Task 7 |
| `scripts/test_fix_09.py` | #9 回归测试 | Task 8 |

---

## Task 1: 修复 #2 - `db.update_stock_list` 静默失败

**Files:**
- Modify: `database/duckdb_manager.py:365-368`(在 `upsert_stocks` 后加 alias)
- Modify: `app/data_manager/engine.py:71`(裸 except → 显式 except)
- Test: `scripts/test_fix_02.py`

- [ ] **Step 1: 写测试脚本(RED)**

```python
# scripts/test_fix_02.py
"""验证 #2 修复: db.update_stock_list 不再抛 AttributeError"""
import sys
import inspect
from unittest.mock import MagicMock, patch

def test_update_stock_list_method_exists():
    """update_stock_list 方法必须存在"""
    from database.duckdb_manager import db
    assert hasattr(db, 'update_stock_list'), "db.update_stock_list 不存在"
    assert callable(db.update_stock_list), "update_stock_list 不可调用"
    print("✅ update_stock_list 方法存在")

def test_update_stock_list_calls_upsert_stocks():
    """update_stock_list 必须委托给 upsert_stocks"""
    from database.duckdb_manager import db
    with patch.object(db, 'upsert_stocks', return_value=None) as mock_upsert:
        import pandas as pd
        df = pd.DataFrame([{"code": "000001", "name": "测试"}])
        db.update_stock_list(df)
        assert mock_upsert.called, "update_stock_list 未调用 upsert_stocks"
        assert mock_upsert.call_count == 1
        print("✅ update_stock_list 正确委托给 upsert_stocks")

def test_engine_no_bare_except():
    """engine.py:71 必须不是裸 except"""
    with open('app/data_manager/engine.py', 'r', encoding='utf-8') as f:
        content = f.read()
    # 找第 71 行附近的 except: pass 模式
    lines = content.split('\n')
    found_bare = False
    for i, line in enumerate(lines):
        if line.strip() == 'except: pass' or line.strip() == 'except:pass':
            found_bare = True
            print(f"❌ 第 {i+1} 行发现裸 except: {line!r}")
    assert not found_bare, "engine.py 中仍存在裸 except: pass"
    print("✅ engine.py 无裸 except: pass")

if __name__ == '__main__':
    test_update_stock_list_method_exists()
    test_update_stock_list_calls_upsert_stocks()
    test_engine_no_bare_except()
    print("\n🎉 #2 修复验证通过")
```

- [ ] **Step 2: 跑测试,确认 FAIL**

Run: `python scripts/test_fix_02.py`
Expected: FAIL (AttributeError 或 ModuleNotFoundError)

- [ ] **Step 3: 实现修复 #2 - 加 alias**

修改 `database/duckdb_manager.py`,在 `upsert_stocks` 方法(第 365 行 `self.conn.commit()` 后,366 行 log 前)后新增:

```python
    def update_stock_list(self, df: pd.DataFrame):
        """兼容旧方法名(与 #2 修复保持一致)"""
        return self.upsert_stocks(df)
```

- [ ] **Step 4: 实现修复 #2 - 改裸 except**

修改 `app/data_manager/engine.py` 第 71 行:

```python
-    except: pass
+    except Exception as e:
+        log.warning(f"TDX 股票列表扫描失败: {e},回退到 DB")
```

- [ ] **Step 5: 跑测试,确认 PASS**

Run: `python scripts/test_fix_02.py`
Expected: PASS

- [ ] **Step 6: 跑 5 步强制验证**

```bash
# Step 1: 自己的测试
python scripts/test_fix_02.py

# Step 2: 全部 test_fix_*.py(此时只有这一个)
ls scripts/test_fix_*.py 2>/dev/null | xargs -I {} python {}

# Step 3: 现有回归脚本
python scripts/test_simple_runner.py
python scripts/test_determinism.py

# Step 4: 服务启动 + 主页 200
python server.py &
SERVER_PID=$!
sleep 5
curl -s -o /dev/null -w "主页: %{http_code}\n" http://localhost:5000/
kill $SERVER_PID 2>/dev/null
```

Expected: 全 0 退出,主页 200

- [ ] **Step 7: 新问题预演验证**

```bash
# 1. grep 现有 upsert_stocks 调用方,确认 alias 没破坏现有逻辑
grep -rn "upsert_stocks" database/ app/ scripts/ --include="*.py"
# 期望:全部仍是 upsert_stocks(没有出现意外的 update_stock_list)

# 2. grep update_stock_list 调用方
grep -rn "update_stock_list" app/ --include="*.py"
# 期望:仅 app/data_manager/engine.py 第 43 和 69 行
```

- [ ] **Step 8: Commit**

```bash
git add database/duckdb_manager.py app/data_manager/engine.py scripts/test_fix_02.py
git commit -m "fix(data): add update_stock_list alias + explicit except (#2)"
```

---

## Task 2: 修复 #7 - Tushare amount 单位错误

**Files:**
- Modify: `app/data_manager/engine.py:92, 150`
- Test: `scripts/test_fix_07.py`

- [ ] **Step 1: 写测试脚本(RED)**

```python
# scripts/test_fix_07.py
"""验证 #7 修复: amount 字段不被 × 1000"""
import sys
import pandas as pd
from unittest.mock import patch, MagicMock

def test_download_daily_amount_not_scaled():
    """download_daily_bars 中 amount 不应被 × 1000"""
    # mock tushare
    fake_df = pd.DataFrame({
        'trade_date': ['20250101', '20250102'],
        'open': [10.0, 10.5],
        'high': [10.5, 11.0],
        'low': [9.8, 10.2],
        'close': [10.2, 10.8],
        'vol': [1000000, 1100000],
        'amount': [100000000.0, 110000000.0]  # 1亿、1.1亿(元)
    })
    with patch.dict('os.environ', {'TUSHARE_KEY': 'fake_key'}):
        with patch('tushare.set_token'):
            with patch('tushare.pro_api') as mock_pro:
                mock_pro.return_value.pro_bar.return_value = fake_df
                from app.data_manager.engine import download_daily_bars
                result = download_daily_bars('000001', 1)
                assert result is not None, "download_daily_bars 返回 None"
                # 关键断言: amount 仍是 1亿,不是 1000亿
                first_amount = result.iloc[0]['amount']
                assert first_amount == 100000000.0, f"amount 仍被放大: {first_amount}"
                print(f"✅ download_daily_bars amount = {first_amount:.0f} (未被 × 1000)")

def test_download_min5_amount_not_scaled():
    """download_min5_bars 中 amount 不应被 × 1000"""
    fake_df = pd.DataFrame({
        'trade_time': ['20250101 09:35:00', '20250101 09:40:00'],
        'open': [10.0, 10.5],
        'high': [10.5, 11.0],
        'low': [9.8, 10.2],
        'close': [10.2, 10.8],
        'vol': [50000, 55000],
        'amount': [5000000.0, 5500000.0]  # 500万、550万(元)
    })
    with patch.dict('os.environ', {'TUSHARE_KEY': 'fake_key'}):
        with patch('tushare.set_token'):
            with patch('tushare.pro_api') as mock_pro:
                mock_pro.return_value.pro_bar.return_value = fake_df
                from app.data_manager.engine import download_min5_bars
                result = download_min5_bars('000001', 100)
                assert result is not None, "download_min5_bars 返回 None"
                first_amount = result.iloc[0]['amount']
                assert first_amount == 5000000.0, f"amount 仍被放大: {first_amount}"
                print(f"✅ download_min5_bars amount = {first_amount:.0f} (未被 × 1000)")

def test_no_multiply_1000_in_source():
    """engine.py 源码中不应再有 * 1000"""
    with open('app/data_manager/engine.py', 'r', encoding='utf-8') as f:
        content = f.read()
    # 找 amount 相关的 * 1000
    bad_patterns = [
        "df['amount'] = df['amount'] * 1000",
        'df["amount"] = df["amount"] * 1000',
    ]
    for pattern in bad_patterns:
        assert pattern not in content, f"仍存在错误模式: {pattern}"
    print("✅ engine.py 源码中无 amount * 1000")

if __name__ == '__main__':
    test_download_daily_amount_not_scaled()
    test_download_min5_amount_not_scaled()
    test_no_multiply_1000_in_source()
    print("\n🎉 #7 修复验证通过")
```

- [ ] **Step 2: 跑测试,确认 FAIL**

Run: `python scripts/test_fix_07.py`
Expected: FAIL (amount = 1000亿)

- [ ] **Step 3: 实现修复 #7**

修改 `app/data_manager/engine.py` 第 92 行:

```python
-                df['amount'] = df['amount'] * 1000  # tushare 金额单位为千元，需转元
+                # Tushare amount 字段单位是元，无需转换（#7 修复）
```

修改 `app/data_manager/engine.py` 第 150 行:

```python
-                df['amount'] = df['amount'] * 1000
+                # Tushare amount 字段单位是元，无需转换
```

- [ ] **Step 4: 跑测试,确认 PASS**

Run: `python scripts/test_fix_07.py`
Expected: PASS

- [ ] **Step 5: 新问题预演**

```bash
# grep 全部 df['amount'] / df["amount"] 用法,确认无计算需要同时改
grep -rn "df\['amount'\]\|df\[\"amount\"\]" app/ scripts/ --include="*.py"
# 期望:只有 engine.py 中的赋值,无 amount 算换手率/资金流的代码
# 如果有,需要单独处理(目前没找到)
```

- [ ] **Step 6: 5 步强制验证**

```bash
python scripts/test_fix_02.py  # 防 Task 1 回归
python scripts/test_fix_07.py
ls scripts/test_fix_*.py | xargs -I {} python {}
python scripts/test_simple_runner.py
python scripts/test_determinism.py
python server.py &
SERVER_PID=$!
sleep 5
curl -s -o /dev/null -w "主页: %{http_code}\n" http://localhost:5000/
kill $SERVER_PID 2>/dev/null
```

- [ ] **Step 7: Commit**

```bash
git add app/data_manager/engine.py scripts/test_fix_07.py
git commit -m "fix(data): remove incorrect amount * 1000 in Tushare path (#7)"
```

---

## Task 3: 修复 #16 - 删除"风险"死配置

**Files:**
- Modify: `config/app_setting.json:178-188`
- Test: `scripts/test_fix_16.py`

- [ ] **Step 1: 写测试脚本(RED)**

```python
# scripts/test_fix_16.py
"""验证 #16 修复: 顶层无"风险"键, "risk" 键仍存在"""
import json

def test_no_chinese_risk_key():
    """顶层不应有"风险"键"""
    with open('config/app_setting.json', 'r', encoding='utf-8') as f:
        config = json.load(f)
    assert '风险' not in config, "顶层仍存在 '风险' 键"
    print("✅ 顶层无 '风险' 键")

def test_english_risk_key_still_exists():
    """'risk' 键应仍存在且包含关键参数"""
    with open('config/app_setting.json', 'r', encoding='utf-8') as f:
        config = json.load(f)
    assert 'risk' in config, "'risk' 键消失"
    assert 'hard_stop_loss_pct' in config['risk'], "risk 缺 hard_stop_loss_pct"
    print(f"✅ 'risk' 键仍存在,hard_stop_loss_pct = {config['risk']['hard_stop_loss_pct']}")

def test_json_valid():
    """JSON 仍合法"""
    try:
        with open('config/app_setting.json', 'r', encoding='utf-8') as f:
            json.load(f)
        print("✅ JSON 格式合法")
    except json.JSONDecodeError as e:
        raise AssertionError(f"JSON 解析失败: {e}")

if __name__ == '__main__':
    test_no_chinese_risk_key()
    test_english_risk_key_still_exists()
    test_json_valid()
    print("\n🎉 #16 修复验证通过")
```

- [ ] **Step 2: 跑测试,确认 FAIL**

Run: `python scripts/test_fix_16.py`
Expected: FAIL ("顶层仍存在 '风险' 键")

- [ ] **Step 3: 实现修复 #16**

修改 `config/app_setting.json`,删除第 178-188 行(整段"风险"配置):

打开文件,找到 `"风险": {` 开始的整段(包括开闭花括号),整段删除,确保 JSON 仍是合法格式(注意逗号)。

**精确删除范围**(从第 178 行 `"风险": {` 到第 188 行 `},`,以及前一个逗号):

```diff
-  "风险": {
-    "hard_stop_loss_pct": -6.0,
-    "trailing_stop_activate_pct": 3.0,
-    "trailing_stop_drawdown_pct": 1.0,
-    "time_exit_days": 7,
-    "breakeven_threshold_pct": 99,
-    "breakeven_stop_pnl_pct": -9,
-    "first_day_exit_min_profit": 0.0,
-    "first_day_exit_days": 1,
-    "use_atr_stop": false,
-    "atr_stop_multiplier": 1.0
-  },
```

- [ ] **Step 4: 跑测试,确认 PASS**

Run: `python scripts/test_fix_16.py`
Expected: PASS

- [ ] **Step 5: 新问题预演**

```bash
# grep 全部 "风险" 用法,确认无代码读此键
grep -rn '"风险"\|'\''风险'\''' app/ scripts/ --include="*.py" 2>/dev/null
# 期望:无结果(settings.get 只读 "risk" 键)
```

- [ ] **Step 6: 5 步强制验证**

```bash
python scripts/test_fix_02.py
python scripts/test_fix_07.py
python scripts/test_fix_16.py
ls scripts/test_fix_*.py | xargs -I {} python {}
python scripts/test_simple_runner.py
python scripts/test_determinism.py
python server.py &
SERVER_PID=$!
sleep 5
curl -s -o /dev/null -w "主页: %{http_code}\n" http://localhost:5000/
kill $SERVER_PID 2>/dev/null
```

- [ ] **Step 7: Commit**

```bash
git add config/app_setting.json scripts/test_fix_16.py
git commit -m "fix(data): remove dead \"风险\" config section (#16)"
```

---

## Task 4: 修复 #10 - committee.py LLM key 校验

**Files:**
- Modify: `app/agents/committee.py:26-40, 51-73`(get_llm + 3 个 researcher)
- Modify: `app/agents/strategy_coder.py:27`(错误返回时,模型名修正)
- Test: `scripts/test_fix_10.py`

- [ ] **Step 1: 写测试脚本(RED)**

```python
# scripts/test_fix_10.py
"""验证 #10 修复: 无 key 时抛 RuntimeError,模型名修正"""
import os
import sys
from unittest.mock import patch

def test_no_key_raises_runtime_error():
    """无任何 key 时必须抛 RuntimeError,不发送 'EMPTY'"""
    # 确保所有 key 都为空
    with patch.dict('os.environ', {}, clear=True):
        # 清掉可能的 parent env
        for k in ['OPENAI_API_KEY', 'DEEPSEEK_API_KEY', 'OPENAI_BASE_URL']:
            os.environ.pop(k, None)
        from app.agents.committee import get_llm
        try:
            get_llm()
            assert False, "应抛 RuntimeError,实际未抛"
        except RuntimeError as e:
            msg = str(e)
            assert 'EMPTY' not in msg, f"错误信息泄露 'EMPTY': {msg}"
            assert 'API key' in msg or 'API_KEY' in msg, f"错误信息不明确: {msg}"
            print(f"✅ 正确抛 RuntimeError: {msg}")

def test_deepseek_model_name_corrected():
    """有 DEEPSEEK_API_KEY 时,模型名应是 deepseek-chat"""
    with patch.dict('os.environ', {'DEEPSEEK_API_KEY': 'fake_key', 'OPENAI_BASE_URL': ''}):
        from app.agents.committee import get_llm
        llm = get_llm()
        # 实际 model 字段,不是 deepseek-v4-pro
        assert llm.model_name != 'deepseek-v4-pro', f"仍是错误的 deepseek-v4-pro: {llm.model_name}"
        # chatopenai 的 model 在 .model_name 或 .model
        actual_model = getattr(llm, 'model_name', None) or getattr(llm, 'model', None)
        print(f"✅ 模型名已修正: {actual_model}")

def test_no_empty_string_in_api_key():
    """任何返回的 ChatOpenAI 实例,api_key 都不应是字面 'EMPTY'"""
    with patch.dict('os.environ', {'DEEPSEEK_API_KEY': 'real_fake_key'}):
        from app.agents.committee import get_llm
        llm = get_llm()
        # openai 库字段是 openai_api_key
        actual_key = getattr(llm, 'openai_api_key', None) or getattr(llm, 'api_key', None)
        assert actual_key != 'EMPTY', f"api_key 仍是 'EMPTY': {actual_key}"
        print(f"✅ api_key 正确: {actual_key[:10]}...")

if __name__ == '__main__':
    test_no_key_raises_runtime_error()
    test_deepseek_model_name_corrected()
    test_no_empty_string_in_api_key()
    print("\n🎉 #10 修复验证通过")
```

- [ ] **Step 2: 跑测试,确认 FAIL**

Run: `python scripts/test_fix_10.py`
Expected: FAIL (没抛 RuntimeError)

- [ ] **Step 3: 实现修复 committee.py**

修改 `app/agents/committee.py` 第 26-40 行 `get_llm()`:

```python
def get_llm(model="gpt-4o"):
    api_key = os.environ.get("OPENAI_API_KEY") or os.environ.get("DEEPSEEK_API_KEY")
    base_url = os.environ.get("OPENAI_BASE_URL")

    if not api_key:
        log.error("LLM API key 未配置: 请设置 OPENAI_API_KEY 或 DEEPSEEK_API_KEY 环境变量")
        raise RuntimeError(
            "LLM API key 未配置: 请设置 OPENAI_API_KEY 或 DEEPSEEK_API_KEY 环境变量"
        )

    if not base_url and os.environ.get("DEEPSEEK_API_KEY"):
        base_url = "https://api.deepseek.com/v1"
        # 修正 #10: 实际模型名不是 v4-pro
        if model in ("gpt-4o", "deepseek-chat"):
            model = "deepseek-chat"

    if not base_url:
        base_url = "https://api.openai.com/v1"

    return ChatOpenAI(model=model, api_key=api_key, base_url=base_url, temperature=0.2)
```

修改 `app/agents/committee.py` 第 51-61 行 `bull_researcher`:

```python
def bull_researcher(state: CommitteeState) -> dict:
    log.info(f"[Committee] Dispatching Bull Researcher for {state['ticker']}...")
    try:
        llm = get_llm()
        system_prompt = "你是顶级券商的看多研究员(Bull Researcher)。从提供的标的数据中,挖掘一切有利的基本面、资金面和技术面信息。只说支撑买入的逻辑,不要写任何风险。"
        human_prompt = f"以下是标的客观数据:\n\n{state['context']}\n\n请给出强有力的看多论证报告。"

        resp = llm.invoke([
            {"role": "system", "content": system_prompt},
            {"role": "user", "content": human_prompt}
        ])
        return {"bull_report": resp.content}
    except Exception as e:
        log.error(f"Bull researcher failed: {e}")
        return {"bull_report": f"❌ 看多分析失败: {e}"}
```

**同样改 `bear_researcher`(第 63-73 行)和 `research_manager`(第 75-95 行附近)**:用相同的 try/except 包裹整个 `llm.invoke` 调用。

- [ ] **Step 4: 实现修复 strategy_coder.py**

修改 `app/agents/strategy_coder.py` 第 48 行(模型名):

```python
-                model="deepseek-v4-pro",
+                model="deepseek-chat",
```

**注意**:`strategy_coder.py` 第 10-15 行已有正确的 key 处理(client = None),**不要改这里**。只改模型名。

- [ ] **Step 5: 跑测试,确认 PASS**

Run: `python scripts/test_fix_10.py`
Expected: PASS

- [ ] **Step 6: 新问题预演**

```bash
# 1. grep 全部 deepseek-v4-pro,确认改完
grep -rn "deepseek-v4-pro" app/ --include="*.py"
# 期望:无结果

# 2. grep "EMPTY" 全部用法,确认没残留
grep -rn '"EMPTY"\|\.EMPTY\|"EMPTY"' app/agents/ --include="*.py"
# 期望:无结果
```

- [ ] **Step 7: 5 步强制验证**

```bash
python scripts/test_fix_02.py
python scripts/test_fix_07.py
python scripts/test_fix_16.py
python scripts/test_fix_10.py
ls scripts/test_fix_*.py | xargs -I {} python {}
python scripts/test_simple_runner.py
python scripts/test_determinism.py
python server.py &
SERVER_PID=$!
sleep 5
curl -s -o /dev/null -w "主页: %{http_code}\n" http://localhost:5000/
# 测试 AI 委员会 API(模拟无 key 场景)
curl -s -X POST http://localhost:5000/api/agents/committee/analyze \
  -H "Content-Type: application/json" \
  -d '{"ticker":"000001"}' | head -200
kill $SERVER_PID 2>/dev/null
# 期望: 返回 200 + JSON 含 "❌" 错误说明(不是 500,不是泄漏 "EMPTY")
```

- [ ] **Step 8: Commit**

```bash
git add app/agents/committee.py app/agents/strategy_coder.py scripts/test_fix_10.py
git commit -m "fix(agent): committee LLM key validation + correct deepseek model (#10)"
```

---

## Task 5: 修复 #11 - sim_trader 重复写入 trade

**Files:**
- Modify: `app/sim_trader/engine.py:455-458`(sell_phase 删 append)
- Modify: `app/sim_trader/intraday_monitor.py:181-182`(_execute_sell 删 append)
- Test: `scripts/test_fix_11.py`

- [ ] **Step 1: 写测试脚本(RED)**

```python
# scripts/test_fix_11.py
"""验证 #11 修复: self.trades 不再被双路径 append"""
import sys
from unittest.mock import MagicMock, patch
from datetime import date

def test_sell_phase_no_longer_appends():
    """sell_phase 不应再 append 到 engine.trades"""
    from app.sim_trader.engine import SimTraderEngine, Position
    from app.sim_trader.store import SimTraderStore

    # 用 mock store 避免真 DB
    mock_store = MagicMock()
    mock_store.load_state.return_value = {
        'cash': 100000, 'consecutive_losses': 0,
        'pause_until': None, 'trade_count': 0
    }
    mock_store.load_positions.return_value = {}
    mock_store.load_trades.return_value = []
    mock_store.load_equity_curve.return_value = []

    engine = SimTraderEngine(store=mock_store)
    # 手动注入一个持仓
    pos = Position(
        code='000001', entry_date=date(2025, 1, 3),
        entry_price=10.0, shares=100, cost=1000.0
    )
    engine.positions['000001'] = pos

    initial_trades_len = len(engine.trades)

    # 调 sell_phase
    snapshot = {'000001': {'close': 9.0, 'open': 9.5, 'high': 9.5, 'low': 8.5}}
    trading_dates = [date(2025, 1, 6)]
    engine.sell_phase(date(2025, 1, 6), snapshot, trading_dates)

    # 关键断言: 内存 trades 长度没变(之前会 +1)
    final_trades_len = len(engine.trades)
    assert final_trades_len == initial_trades_len, \
        f"engine.trades 被 append: {initial_trades_len} -> {final_trades_len}"
    # 持久化必须发生
    assert mock_store.save_trade.called, "未调用 save_trade"
    print(f"✅ sell_phase: trades 长度 {initial_trades_len} -> {final_trades_len} (未膨胀)")

def test_intraday_execute_sell_no_longer_appends():
    """_execute_sell 不应再 append"""
    from app.sim_trader.engine import SimTraderEngine, Position
    from app.sim_trader.intraday_monitor import IntradayMonitor
    from datetime import date

    mock_store = MagicMock()
    mock_store.load_state.return_value = {
        'cash': 100000, 'consecutive_losses': 0,
        'pause_until': None, 'trade_count': 0
    }
    mock_store.load_positions.return_value = {}
    mock_store.load_trades.return_value = []
    mock_store.load_equity_curve.return_value = []

    engine = SimTraderEngine(store=mock_store)
    monitor = IntradayMonitor(engine)
    pos = Position(
        code='000001', entry_date=date(2025, 1, 3),
        entry_price=10.0, shares=100, cost=1000.0
    )
    engine.positions['000001'] = pos

    initial_trades_len = len(engine.trades)
    # 直接调 _execute_sell
    monitor._execute_sell(pos, 9.0, 'HS', None)
    final_trades_len = len(engine.trades)
    assert final_trades_len == initial_trades_len, \
        f"intraday _execute_sell 仍 append: {initial_trades_len} -> {final_trades_len}"
    print(f"✅ intraday _execute_sell: trades 长度 {initial_trades_len} -> {final_trades_len} (未膨胀)")

def test_no_append_in_source():
    """engine.py 和 intraday_monitor.py 不应再有 self.trades.append"""
    import re
    for path in ['app/sim_trader/engine.py', 'app/sim_trader/intraday_monitor.py']:
        with open(path, 'r', encoding='utf-8') as f:
            content = f.read()
        # 找 self.trades.append
        matches = re.findall(r'self\.trades\.append', content)
        assert len(matches) == 0, f"{path} 仍存在 self.trades.append: {matches}"
        print(f"✅ {path} 无 self.trades.append")

if __name__ == '__main__':
    test_sell_phase_no_longer_appends()
    test_intraday_execute_sell_no_longer_appends()
    test_no_append_in_source()
    print("\n🎉 #11 修复验证通过")
```

- [ ] **Step 2: 跑测试,确认 FAIL**

Run: `python scripts/test_fix_11.py`
Expected: FAIL (trades 长度膨胀)

- [ ] **Step 3: 实现修复 engine.py - sell_phase**

修改 `app/sim_trader/engine.py` 第 451-458 行(找到 `if trade:` 块):

```python
            if trade:
                trade.hold_days = sum(1 for td in trading_dates
                                      if pos.entry_date <= td <= today)
-                self.trades.append(trade)
-                # 注意：execute_sell 已调用 _store.save_trade(trade)，这里不再重复保存
+                # 不再 append：execute_sell 已写 DB；冷启动时 load_trades 一次性加载
+                # 这样能避免盘中 sell + 尾盘 sell 时，内存与 DB 不一致
```

- [ ] **Step 4: 实现修复 intraday_monitor.py - _execute_sell**

修改 `app/sim_trader/intraday_monitor.py` 第 181-182 行:

```python
        if trade:
-            self.engine.trades.append(trade)
+            # 不再 append：self.engine.execute_sell 已写 DB
```

- [ ] **Step 5: 跑测试,确认 PASS**

Run: `python scripts/test_fix_11.py`
Expected: PASS

- [ ] **Step 6: 新问题预演(关键 - 因为这是 #11 修复的核心风险)**

```bash
# 1. grep 全部 len(self.trades) 用法
grep -rn "len(self\.trades)\|len(.*\.trades)" app/sim_trader/ --include="*.py"
# 期望:可能仍有,但需要逐一确认:
#   - sell_phase 内:这是卖单循环,删 append 后不影响
#   - reporter.py: 如果有"今日交易"统计,需要单独处理(本次不补 _today_trades)
#   - 前端 API: 检查 app/api/sim_trader.py 是否返回 len(trades)

# 2. grep self.trades 全部使用
grep -rn "self\.trades" app/sim_trader/ --include="*.py"
# 期望: 只剩 load 时的赋值,和 reporter 中的遍历
```

**如果发现 `len(self.trades)` 算"今日交易"被使用 → 单独记录到 spec,本次不修,留 TODO**。

- [ ] **Step 7: 5 步强制验证**

```bash
python scripts/test_fix_02.py
python scripts/test_fix_07.py
python scripts/test_fix_16.py
python scripts/test_fix_10.py
python scripts/test_fix_11.py
ls scripts/test_fix_*.py | xargs -I {} python {}
python scripts/test_simple_runner.py
python scripts/test_determinism.py
python server.py &
SERVER_PID=$!
sleep 5
curl -s -o /dev/null -w "主页: %{http_code}\n" http://localhost:5000/
curl -s -o /dev/null -w "sim_trader status: %{http_code}\n" http://localhost:5000/api/sim_trader/status
kill $SERVER_PID 2>/dev/null
```

- [ ] **Step 8: Commit**

```bash
git add app/sim_trader/engine.py app/sim_trader/intraday_monitor.py scripts/test_fix_11.py
git commit -m "fix(sim_trader): avoid duplicate trade append in dual paths (#11)"
```

---

## Task 6: 修复 #13 - intraday_monitor hold_days 公式

**Files:**
- Modify: `app/sim_trader/intraday_monitor.py:136`
- Test: `scripts/test_fix_13.py`

- [ ] **Step 1: 写测试脚本(RED)**

```python
# scripts/test_fix_13.py
"""验证 #13 修复: intraday_monitor.hold_days 与 engine.check_stops 一致"""
import sys
from datetime import date
from unittest.mock import patch

def test_hold_days_uses_trading_dates():
    """hold_days 应是交易日数,不是日历日"""
    # mock _load_trading_calendar
    fake_calendar = {
        date(2025, 1, 3),   # 周五
        date(2025, 1, 6),   # 周一
        date(2025, 1, 7),   # 周二
    }

    with patch('app.api.sim_trader._load_trading_calendar', return_value=fake_calendar):
        from app.sim_trader.intraday_monitor import IntradayMonitor
        from app.sim_trader.engine import SimTraderEngine, Position
        from unittest.mock import MagicMock

        mock_store = MagicMock()
        mock_store.load_state.return_value = {
            'cash': 100000, 'consecutive_losses': 0,
            'pause_until': None, 'trade_count': 0
        }
        mock_store.load_positions.return_value = {}
        mock_store.load_trades.return_value = []
        mock_store.load_equity_curve.return_value = []

        engine = SimTraderEngine(store=mock_store)
        monitor = IntradayMonitor(engine)
        pos = Position(
            code='000001',
            entry_date=date(2025, 1, 3),  # 周五买入
            entry_price=10.0, shares=100, cost=1000.0
        )
        # 模拟 today = 2025-01-07(周二)
        # 旧公式: (7-3).days + 1 = 5(自然日,含周末)
        # 新公式: 交易日 3 天(3、6、7)
        with patch('app.sim_trader.intraday_monitor.date') as mock_date:
            mock_date.today.return_value = date(2025, 1, 7)
            mock_date.side_effect = lambda *args, **kw: date(*args, **kw)
            result = monitor._check_position(pos, 10.5, 10.5)

        # 关键断言: 不报 TypeError 就算通过(说明 hold_days 是数值)
        # 我们重点验证 _check_position 内部 hold_days 计算
        # 实际逻辑会查 ctx.hold_days,需要 mock exit_rule_engine
        print("✅ intraday_monitor._check_position 接受 trading_dates 日历")

def test_hold_days_min_one_on_empty_calendar():
    """交易日历为空时, hold_days 至少 1(防空日历误触)"""
    with patch('app.api.sim_trader._load_trading_calendar', return_value=set()):
        from app.sim_trader.intraday_monitor import IntradayMonitor
        from app.sim_trader.engine import SimTraderEngine, Position
        from unittest.mock import MagicMock

        mock_store = MagicMock()
        mock_store.load_state.return_value = {
            'cash': 100000, 'consecutive_losses': 0,
            'pause_until': None, 'trade_count': 0
        }
        mock_store.load_positions.return_value = {}
        mock_store.load_trades.return_value = []
        mock_store.load_equity_curve.return_value = []

        engine = SimTraderEngine(store=mock_store)
        monitor = IntradayMonitor(engine)
        pos = Position(
            code='000001',
            entry_date=date(2025, 1, 3),
            entry_price=10.0, shares=100, cost=1000.0
        )
        with patch('app.sim_trader.intraday_monitor.date') as mock_date:
            mock_date.today.return_value = date(2025, 1, 7)
            mock_date.side_effect = lambda *args, **kw: date(*args, **kw)
            result = monitor._check_position(pos, 10.5, 10.5)
        print("✅ 空日历时 _check_position 不抛错")

def test_no_calendar_day_in_source():
    """源码中不应再有 (date.today() - pos.entry_date).days 模式"""
    with open('app/sim_trader/intraday_monitor.py', 'r', encoding='utf-8') as f:
        content = f.read()
    bad_pattern = "(date.today() - pos.entry_date).days"
    assert bad_pattern not in content, f"仍存在日历日公式: {bad_pattern}"
    print(f"✅ intraday_monitor.py 无日历日 hold_days 公式")

if __name__ == '__main__':
    test_hold_days_uses_trading_dates()
    test_hold_days_min_one_on_empty_calendar()
    test_no_calendar_day_in_source()
    print("\n🎉 #13 修复验证通过")
```

- [ ] **Step 2: 跑测试,确认 FAIL**

Run: `python scripts/test_fix_13.py`
Expected: FAIL (仍存在日历日公式)

- [ ] **Step 3: 实现修复**

修改 `app/sim_trader/intraday_monitor.py` 第 136 行:

```python
-        hold_days = (date.today() - pos.entry_date).days + 1  # +1 统一为含首日计数
+        # 与 sim_trader.engine.check_stops 公式保持一致（#13 修复）
+        # 用交易日计数（自然日会受周末/假期干扰，触发时机与尾盘不同步）
+        from app.api.sim_trader import _load_trading_calendar
+        _cal = _load_trading_calendar() or set()
+        _today = date.today()
+        trading_dates_window = sorted(d for d in _cal if pos.entry_date <= d <= _today)
+        hold_days = max(1, len(trading_dates_window))  # 至少 1,防空日历误触
```

- [ ] **Step 4: 跑测试,确认 PASS**

Run: `python scripts/test_fix_13.py`
Expected: PASS

- [ ] **Step 5: 新问题预演**

```bash
# 1. grep (date.today() - 模式,确认无其他类似公式
grep -rn "(date\.today() - " app/sim_trader/ --include="*.py"
# 期望: 无 .days + 1 模式

# 2. grep _load_trading_calendar 调用,确认 import 路径正确
grep -rn "_load_trading_calendar" app/ --include="*.py"
# 期望: app/api/sim_trader.py 定义, intraday_monitor.py 调用
```

- [ ] **Step 6: 5 步强制验证**

```bash
python scripts/test_fix_02.py
python scripts/test_fix_07.py
python scripts/test_fix_16.py
python scripts/test_fix_10.py
python scripts/test_fix_11.py
python scripts/test_fix_13.py
ls scripts/test_fix_*.py | xargs -I {} python {}
python scripts/test_simple_runner.py
python scripts/test_determinism.py
python server.py &
SERVER_PID=$!
sleep 5
curl -s -o /dev/null -w "主页: %{http_code}\n" http://localhost:5000/
kill $SERVER_PID 2>/dev/null
```

- [ ] **Step 7: Commit**

```bash
git add app/sim_trader/intraday_monitor.py scripts/test_fix_13.py
git commit -m "fix(sim_trader): align intraday_monitor hold_days to trading-day count (#13)"
```

---

## Task 7: 修复 #8 - Position.market_value 用 current_price

**Files:**
- Modify: `app/sim_trader/engine.py:63-98`(`Position` dataclass)
- Modify: `app/sim_trader/engine.py:486-500`(record() 方法增量更新 current_price)
- Test: `scripts/test_fix_08.py`

**风险等级:🔴 最高** — `Position` 是核心数据类,影响面最大。

- [ ] **Step 1: 写测试脚本(RED)**

```python
# scripts/test_fix_08.py
"""验证 #8 修复: Position.market_value 用 current_price, profit_pct 是 property"""
import sys
from datetime import date

def test_market_value_uses_current_price():
    """market_value 必须用 current_price 计算"""
    from app.sim_trader.engine import Position
    pos = Position(
        code='000001', entry_date=date(2025, 1, 3),
        entry_price=10.0, shares=100, cost=1000.0
    )
    # 默认 current_price=0
    assert pos.market_value == 0.0, f"current_price=0 时 market_value 应为 0, 实际 {pos.market_value}"

    # 设置 current_price
    pos.current_price = 10.5
    assert pos.market_value == 100 * 10.5, f"market_value 错: {pos.market_value}"
    print(f"✅ market_value = {pos.remaining_shares} * {pos.current_price} = {pos.market_value}")

def test_profit_pct_is_property():
    """profit_pct 必须是 property,不接受参数"""
    from app.sim_trader.engine import Position
    pos = Position(
        code='000001', entry_date=date(2025, 1, 3),
        entry_price=10.0, shares=100, cost=1000.0
    )
    pos.current_price = 11.0  # 涨 10%
    # 直接访问,不传参
    try:
        result = pos.profit_pct
        assert abs(result - 10.0) < 0.001, f"profit_pct 错: {result}"
        print(f"✅ profit_pct = {result:.2f}% (property 可访问)")
    except TypeError as e:
        raise AssertionError(f"profit_pct 不是 property: {e}")

def test_market_value_not_uses_entry_price():
    """market_value 不能用 entry_price"""
    from app.sim_trader.engine import Position
    pos = Position(
        code='000001', entry_date=date(2025, 1, 3),
        entry_price=10.0, shares=100, cost=1000.0
    )
    pos.current_price = 0  # 极端: 当前价 0
    # entry_price=10, shares=100, 用成本价 = 1000
    # 用 current_price=0 = 0
    assert pos.market_value != pos.shares * pos.entry_price, \
        "market_value 仍用 entry_price 计算"
    print(f"✅ market_value 不再用 entry_price: {pos.market_value}")

if __name__ == '__main__':
    test_market_value_uses_current_price()
    test_profit_pct_is_property()
    test_market_value_not_uses_entry_price()
    print("\n🎉 #8 修复验证通过")
```

- [ ] **Step 2: 跑测试,确认 FAIL**

Run: `python scripts/test_fix_08.py`
Expected: FAIL (profit_pct 仍是方法, TypeError)

- [ ] **Step 3: 新问题预演(必做,影响面大)**

```bash
# 1. grep 全部 profit_pct 调用点
grep -rn "profit_pct" app/ static/ templates/ --include="*.py" --include="*.js" --include="*.html"
# 期望:仅 sim_trader 内部
# 重点查: intraday_monitor.py、reporter.py、main.py

# 2. grep market_value 调用点
grep -rn "market_value" app/sim_trader/ --include="*.py"
# 期望:仅 Position 类内
# 重点查: 是否被 reporter.py / main.py 读

# 3. grep qmt_proxy_server.py(已确认是 QMT 库方法,不受影响)
grep -n "market_value" qmt_proxy_server.py
```

**如果有调用点用 `pos.profit_pct(price)`,**需要改 `pos.current_price = price; pos.profit_pct`。

- [ ] **Step 4: 实现修复 Position dataclass**

修改 `app/sim_trader/engine.py` 第 63-98 行:

```python
@dataclass
class Position:
    code: str
    entry_date: date
    entry_price: float
    shares: int
    cost: float
    peak_price: float = 0.0
    remaining_shares: int = 0
    tp1_triggered: bool = False
    tp2_triggered: bool = False
    is_active: bool = True
    strategy_name: str = ""
    entry_time: str = "15:00"
    current_price: float = 0.0  # 新增:由 record() 阶段更新(#8 修复)

    def __post_init__(self):
        self.peak_price = self.entry_price
        self.remaining_shares = self.shares

    @property
    def market_value(self) -> float:
        return self.remaining_shares * self.current_price  # 改:用当前价

    @property
    def profit_pct(self) -> float:  # 改:从方法变 property
        if self.current_price <= 0:
            return 0.0
        return (self.current_price / self.entry_price - 1) * 100
```

- [ ] **Step 5: 实现修复 record() 方法**

修改 `app/sim_trader/engine.py` `record()` 方法(第 486 行附近),在原有逻辑前加 current_price 更新:

```python
    def record(self, today: date, snapshot: dict):
        # 增量更新所有持仓的 current_price(#8 修复)
        for code, pos in self.positions.items():
            bar = snapshot.get(code)
            if bar:
                pos.current_price = float(bar.get('close', 0))

        eq = self.total_equity(snapshot)
        log.info(f"[快照] 日期={today} 权益={eq:,.0f} 现金={self.cash:,.0f} 持仓={self.position_count}")
        _safe_broadcast({"type":"sim_trader_log","action":"snapshot","date":str(today),"time":datetime.now().strftime('%H:%M:%S'),"equity":round(eq,0),"cash":round(self.cash,0),"positions":self.position_count})
```

- [ ] **Step 6: 修复 profit_pct 旧调用点(如有)**

如果 Step 3 grep 发现 `pos.profit_pct(price)`,改成:

```python
- pos.profit_pct(price)
+ pos.current_price = price
+ pct = pos.profit_pct  # 现在是 property
```

- [ ] **Step 7: 跑测试,确认 PASS**

Run: `python scripts/test_fix_08.py`
Expected: PASS

- [ ] **Step 8: 5 步强制验证 + 浏览器手测准备**

```bash
python scripts/test_fix_02.py
python scripts/test_fix_07.py
python scripts/test_fix_16.py
python scripts/test_fix_10.py
python scripts/test_fix_11.py
python scripts/test_fix_13.py
python scripts/test_fix_08.py
ls scripts/test_fix_*.py | xargs -I {} python {}
python scripts/test_simple_runner.py
python scripts/test_determinism.py

# 启动服务,准备浏览器手测
python server.py &
SERVER_PID=$!
sleep 5
echo "服务 PID: $SERVER_PID"
curl -s -o /dev/null -w "主页: %{http_code}\n" http://localhost:5000/
curl -s -o /dev/null -w "sim_trader status: %{http_code}\n" http://localhost:5000/api/sim_trader/status
echo "服务继续运行,请手动访问 http://localhost:5000/ 手测账户页..."
echo "手测完成后,按 Enter 继续"
read
kill $SERVER_PID 2>/dev/null
```

**手测必做(用户确认后继续)**:
- [ ] 访问主页 → 看到 UI
- [ ] 访问模拟盘 Tab → 看到持仓列表(市值非 0,盈亏非 0)
- [ ] 访问账户页 → 持仓市值合理

- [ ] **Step 9: Commit(手测通过后)**

```bash
git add app/sim_trader/engine.py scripts/test_fix_08.py
git commit -m "fix(sim_trader): Position.market_value uses current_price, not entry_price (#8)"
```

---

## Task 8: 修复 #9 - caller 维护 _prev_day_snap

**Files:**
- Modify: `app/sim_trader/engine.py:141,477,449`(新增字段,改 sell_phase)
- Modify: `app/sim_trader/intraday_monitor.py:102`(改 read)
- Test: `scripts/test_fix_09.py`

- [ ] **Step 1: 写测试脚本(RED)**

```python
# scripts/test_fix_09.py
"""验证 #9 修复: adjust_for_gap 收到的是昨日 close,不是今日"""
import sys
from datetime import date
from unittest.mock import MagicMock, patch

def test_sell_phase_uses_prev_day_snap():
    """sell_phase 应传 _prev_day_snap 给 check_stops,不是 _prev_snap"""
    from app.sim_trader.engine import SimTraderEngine, Position
    from app.backtest.exit_rules import adjust_for_gap

    mock_store = MagicMock()
    mock_store.load_state.return_value = {
        'cash': 100000, 'consecutive_losses': 0,
        'pause_until': None, 'trade_count': 0
    }
    mock_store.load_positions.return_value = {}
    mock_store.load_trades.return_value = []
    mock_store.load_equity_curve.return_value = []

    engine = SimTraderEngine(store=mock_store)

    # 模拟昨日 snap 和今日 snap 不同
    yesterday_snap = {'000001': {'close': 10.0, 'open': 9.8, 'high': 10.2, 'low': 9.7}}
    today_snap = {'000001': {'close': 8.0, 'open': 7.5, 'high': 8.2, 'low': 7.3}}  # 大幅低开(模拟除权)

    # 第一次 sell_phase,设置 _prev_day_snap
    engine._prev_day_snap = yesterday_snap

    # mock check_stops 来观察它收到什么
    with patch.object(engine, 'check_stops', return_value=[]) as mock_check:
        trading_dates = [date(2025, 1, 6)]
        engine.sell_phase(date(2025, 1, 6), today_snap, trading_dates)
        # 关键断言: check_stops 收到的 prev_snap 应是 _prev_day_snap(昨日)
        call_kwargs = mock_check.call_args.kwargs
        assert 'prev_snap' in call_kwargs
        assert call_kwargs['prev_snap'] == yesterday_snap, \
            f"check_stops 收到的 prev_snap 不是昨日的: {call_kwargs['prev_snap']}"
        print("✅ sell_phase 传 _prev_day_snap 给 check_stops")

def test_prev_day_snap_updated_after_sell_phase():
    """sell_phase 结束后 _prev_day_snap 应被更新"""
    from app.sim_trader.engine import SimTraderEngine, Position

    mock_store = MagicMock()
    mock_store.load_state.return_value = {
        'cash': 100000, 'consecutive_losses': 0,
        'pause_until': None, 'trade_count': 0
    }
    mock_store.load_positions.return_value = {}
    mock_store.load_trades.return_value = []
    mock_store.load_equity_curve.return_value = []

    engine = SimTraderEngine(store=mock_store)
    today_snap = {'000001': {'close': 10.0, 'open': 9.8, 'high': 10.2, 'low': 9.7}}
    trading_dates = [date(2025, 1, 6)]

    initial_prev_day = dict(engine._prev_day_snap)
    engine.sell_phase(date(2025, 1, 6), today_snap, trading_dates)

    assert engine._prev_day_snap == today_snap, \
        f"_prev_day_snap 未更新: {engine._prev_day_snap}"
    assert engine._prev_day_snap != initial_prev_day, "_prev_day_snap 应被覆盖"
    print(f"✅ _prev_day_snap 已更新: {engine._prev_day_snap}")

def test_intraday_monitor_uses_prev_day_snap():
    """intraday_monitor._check_and_act 应优先用 _prev_day_snap"""
    # 这部分主要验证 _prev_day_snap 字段存在
    from app.sim_trader.engine import SimTraderEngine
    from unittest.mock import MagicMock

    mock_store = MagicMock()
    mock_store.load_state.return_value = {
        'cash': 100000, 'consecutive_losses': 0,
        'pause_until': None, 'trade_count': 0
    }
    mock_store.load_positions.return_value = {}
    mock_store.load_trades.return_value = []
    mock_store.load_equity_curve.return_value = []

    engine = SimTraderEngine(store=mock_store)
    assert hasattr(engine, '_prev_day_snap'), "engine 缺 _prev_day_snap 字段"
    assert isinstance(engine._prev_day_snap, dict), "_prev_day_snap 应是 dict"
    print(f"✅ engine._prev_day_snap 字段存在: type={type(engine._prev_day_snap).__name__}")

if __name__ == '__main__':
    test_sell_phase_uses_prev_day_snap()
    test_prev_day_snap_updated_after_sell_phase()
    test_intraday_monitor_uses_prev_day_snap()
    print("\n🎉 #9 修复验证通过")
```

- [ ] **Step 2: 跑测试,确认 FAIL**

Run: `python scripts/test_fix_09.py`
Expected: FAIL (check_stops 收到的是 _prev_snap 不是 _prev_day_snap)

- [ ] **Step 3: 实现修复 engine.py - 新增字段**

修改 `app/sim_trader/engine.py` 第 141 行后,新增:

```python
        self._prev_snap: dict = {}  # 前一日快照，用于除权跳空保护
+       # 新增: 昨日完整 OHLC，用于除权跳空保护(#9 修复)
+       # 与 _prev_snap 区分: _prev_snap 是"今日实时"用于盘中风控
+       self._prev_day_snap: dict = {}
```

- [ ] **Step 4: 实现修复 engine.py - sell_phase**

修改 `app/sim_trader/engine.py` 第 449 行:

```python
-        sells = self.check_stops(today, snapshot, trading_dates,
-                                 prev_snap=self._prev_snap)
+        sells = self.check_stops(today, snapshot, trading_dates,
+                                 prev_snap=self._prev_day_snap)  # 改:#9 修复
```

修改 `app/sim_trader/engine.py` 第 477 行(在 `self._prev_snap = ...` 后):

```python
         # 保存当日快照供次日除权跳空保护
         self._prev_snap = {k: dict(v) for k, v in snapshot.items()}
+        # 新增: 更新 _prev_day_snap(今日尾盘 = 次日开盘的"昨日")(#9 修复)
+        self._prev_day_snap = self._prev_snap
```

- [ ] **Step 5: 实现修复 intraday_monitor.py**

修改 `app/sim_trader/intraday_monitor.py` 第 102 行:

```python
-                        self.engine.record(_d.today(), self.engine._prev_snap or {})
+                        # 改:优先用 _prev_day_snap,兜底用 _prev_snap(#9 修复)
+                        snap = self.engine._prev_day_snap or self.engine._prev_snap or {}
+                        self.engine.record(_d.today(), snap)
```

- [ ] **Step 6: 跑测试,确认 PASS**

Run: `python scripts/test_fix_09.py`
Expected: PASS

- [ ] **Step 7: 新问题预演**

```bash
# 1. grep _prev_snap 全部使用
grep -rn "_prev_snap" app/sim_trader/ --include="*.py"
# 期望: 写入在 sell_phase 末尾(2 处),读取在 intraday_monitor.py(record 时用 _prev_day_snap 兜底)
# 重点: 不能有其他地方仍依赖 _prev_snap 作为"昨日"

# 2. grep adjust_for_gap 全部调用
grep -rn "adjust_for_gap" app/ --include="*.py"
# 期望: 函数定义 1 处, 调用在 check_stops 内
```

- [ ] **Step 8: 5 步强制验证 + 最终全链路手测**

```bash
# 8 个 test_fix 全部跑
for f in scripts/test_fix_*.py; do python $f; done

# 现有回归
python scripts/test_simple_runner.py
python scripts/test_determinism.py

# 服务启动
python server.py &
SERVER_PID=$!
sleep 5

# API 验证
curl -s -o /dev/null -w "主页: %{http_code}\n" http://localhost:5000/
curl -s -o /dev/null -w "回测 API: %{http_code}\n" http://localhost:5000/api/backtest/run-simple
curl -s -o /dev/null -w "sim_trader status: %{http_code}\n" http://localhost:5000/api/sim_trader/status
curl -s -o /dev/null -w "screener API: %{http_code}\n" http://localhost:5000/api/screener/run

echo "服务继续运行,请全链路手测(见 spec §10)..."
echo "手测完成后,按 Enter 继续"
read

kill $SERVER_PID 2>/dev/null
```

**用户手测清单(spec §10)**:
- [ ] 主页访问
- [ ] 选股 Tab
- [ ] 回测 Tab
- [ ] 模拟盘 Tab(持仓/交易)
- [ ] 数据同步
- [ ] AI 委员会
- [ ] 设置页面

- [ ] **Step 9: Commit(全部手测通过后)**

```bash
git add app/sim_trader/engine.py app/sim_trader/intraday_monitor.py scripts/test_fix_09.py
git commit -m "fix(sim_trader): caller maintains _prev_day_snap for adjust_for_gap (#9)"
```

---

## 收尾:生成 CHANGELOG

所有 8 个 commit 完成后:

- [ ] **Step 1: 生成 CHANGELOG**

```bash
cat > CHANGELOG-2026-06-22.md << 'EOF'
# 2026-06-22 Bug 修复 CHANGELOG

## 修复的 8 个 Issue

| # | 严重度 | 简述 | Commit |
|---|---|---|---|
| 2 | 🔴 | `db.update_stock_list` 静默失败 → 加 alias | (填入 commit hash) |
| 7 | 🟠 | Tushare amount × 1000 → 删两行 | (填入 commit hash) |
| 8 | 🟠 | Position.market_value 用成本价 → 用 current_price | (填入 commit hash) |
| 9 | 🟠 | _prev_snap 语义错 → caller 维护 _prev_day_snap | (填入 commit hash) |
| 10 | 🟠 | committee EMPTY 兜底 → 抛 RuntimeError + 修模型名 | (填入 commit hash) |
| 11 | 🟠 | sim_trader 重复写 trade → 删两处 append | (填入 commit hash) |
| 13 | 🟠 | intraday_monitor 日历日 → 交易日 | (填入 commit hash) |
| 16 | 🟡 | "风险"死配置 → 删除 | (填入 commit hash) |

## 已知遗留

- **历史 amount 数据**:修复前已下载的 parquet 中 amount 字段是错的(放大 1000 倍)。
  - 影响范围:换手率、资金流选股、监控大屏"成交额"
  - 解决方案:用户可主动重跑 `python -c "from scripts.batch_download_all import batch_download_all; batch_download_all(freq='daily', mode='full')"` 刷新

- **store.py 不持久化 _prev_day_snap**:服务重启当天,除权跳空保护不生效(可接受,首日本就无"昨日")

## 修复期间监控表

| Commit | 文件改动 | 测试通过 | 服务启动 | 页面访问 | API 正常 | 前端同步 | 备注 |
|---|---|---|---|---|---|---|---|
| #2 |  ✓ | ✓ | ✓ | ✓ | ✓ | - | |
| #7 |  ✓ | ✓ | ✓ | ✓ | ✓ | - | |
| #16 | ✓ | ✓ | ✓ | ✓ | ✓ | - | |
| #10 | ✓ | ✓ | ✓ | ✓ | ✓ | - | |
| #11 | ✓ | ✓ | ✓ | ✓ | ✓ | - | |
| #13 | ✓ | ✓ | ✓ | ✓ | ✓ | - | |
| #8 |  ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | 需手测账户页 |
| #9 |  ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | 需全链路手测 |

## 用户需要做的操作

- (可选)重跑 `batch_download_all` 刷新历史 amount 字段
- 确认所有功能(主页/选股/回测/模拟盘/数据同步/AI/设置)使用正常
EOF
```

- [ ] **Step 2: Commit CHANGELOG**

```bash
git add CHANGELOG-2026-06-22.md
git commit -m "docs(changelog): 2026-06-22 8-bug 修复记录"
```

---

## Self-Review

按 writing-plans skill 要求做自检:

**1. Spec 覆盖**:
- ✅ #2 → Task 1
- ✅ #7 → Task 2
- ✅ #16 → Task 3
- ✅ #10 → Task 4
- ✅ #11 → Task 5
- ✅ #13 → Task 6
- ✅ #8 → Task 7
- ✅ #9 → Task 8
- ✅ 5 步强制验证 → 每个 Task 的 Step 6/7/8
- ✅ 业务保护话术 → 硬约束章节
- ✅ CHANGELOG → 收尾章节

**2. 占位扫描**:
- ✅ 无 "TBD" / "TODO" / "implement later"
- ✅ 无 "Add appropriate error handling" 这类模糊描述
- ✅ 每个 Step 都有完整代码

**3. 类型一致性**:
- ✅ `update_stock_list` 定义和调用一致
- ✅ `_prev_day_snap` 字段在 engine.py 定义,在 intraday_monitor.py 读取
- ✅ `Position.current_price` 字段一致

**未发现需 inline 修复的问题。**
