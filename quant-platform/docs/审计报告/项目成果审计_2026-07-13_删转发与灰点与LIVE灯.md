# 项目成果审计报告 — 删实盘信号转发 + 监控灰点 + LIVE灯文字

> 审计日期: 2026-07-13
> 审计范围: 仅今天这轮改动（用户指定）
> 审计维度: 功能正确性 + 代码质量 + 一致性（不含安全）
> 审计方法: 三角色（架构/功能/测试）+ 交互响应 + 穷举隐患，全部 Read/Grep 实际代码验证

---

## 审计对象

### 改动范围（7 文件，净删 ~319 行）

| 文件 | 改动 |
|---|---|
| `static/index.html` | 删第四开关 DOM（乱码 span + 分隔符）+ bump `main.js?v=42` `live_trader.js?v=6` |
| `static/js/main.js` | 删 4 处 live-signal 逻辑 + 加 2 处监控灰点变色 |
| `static/js/live_trader.js` | LIVE 灯 mode 文字映射改清楚（live→实盘·真钱 / dry-run→模拟·不下单） |
| `app/sim_trader/config.py` | 删 `LIVE_SIGNAL_MODE` 定义 + 注释 |
| `app/api/sim_trader.py` | 删 6 处（入参/持久化/log/status/`_get_live_signal_mode` 函数/手动触发块） |
| `app/scheduler/cron_jobs.py` | 删 257 行（触发块 + `_forward_signals_to_live_trader`/`_update_signal_state`/`_cleanup_signal_state`/`_state_lock`） |
| `config/app_setting.json` | 删 `sim_trader.live_signal_mode` 字段（运行中进程又写回） |

### 原始要求清单（从对话还原）

1. **监控灰点为何灰色** → 用户选"跟开关走"：开=绿，关=灰
2. **买入后面乱码** → 查实为废弃的"实盘信号转发"开关，用户选 B 彻底删除
3. **LIVE 灯红色像故障** → 查实为 mode 指示灯（有用），用户选 A 改清楚文字保留红色

---

## ✅ 要求 vs 实现比对

| 要求 | 实现情况 | 证据 | 结论 |
|---|---|---|---|
| 灰点跟 monitor_enabled 变色 | loadSimMonitor + saveSimMonitor 各加一行 dot 变色 | main.js:679-680, 704-705 | ✅ |
| 实盘信号转发彻底删除 | 6 文件清理，全项目 grep（.py/.js/.html/.json）零残留 | 全代码库扫描 | ✅ |
| LIVE 灯文字改清楚 | live→"实盘·真钱"(红) / dry-run→"模拟·不下单"(橙) | live_trader.js:29 | ✅ |
| 不破坏现有功能 | 47 测试通过 + py_compile×3 + node -c×2 + JSON 校验 | pytest 实跑 | ✅ |
| 前后端契约一致 | status 不再返回 live_signal_mode，前端不再读 | sim_trader.py:287 / main.js:715 | ✅ |

---

## 🏛️ 架构分析师发现

### ✅ 向后兼容（无破坏）
- 旧前端缓存若仍发 `live_signal_mode` 字段 → 后端 `if 'live_signal_mode' in body` 已删，直接忽略不报错（sim_trader.py:134-141 只处理 auto_*/monitor_*/strategy_name）
- 旧 `app_setting.json` 若残留 `live_signal_mode` → `settings.get` 不读它，无害
- bump 版本号（main.js?v=42, live_trader.js?v=6）确保浏览器拉新版

### ✅ cron_jobs.py 结构完整
- daily 任务（`run_sim_trader_daily`）删除触发块 + cleanup 调用后，结构闭环：买入逻辑 → 净值快照 → broadcast → log → except（cron_jobs.py:497-524）
- `pipeline_scheduler = DataPipelineScheduler()` 单例仍正常（cron_jobs.py:529）
- `_state_lock` 类属性删除后，`__init__` 和其他方法无引用（grep 全项目零残留），运行时不会 AttributeError

### ✅ sim_trader.py 结构完整
- status 接口 dict 删除 live_signal_mode 后逗号/闭合正确（sim_trader.py:287-288）
- 手动触发块删除后，外层 try/except 配对完整（sim_trader.py:370 except 配对）
- `_get_live_signal_mode` 函数及其调用一并删除，无悬空引用

### ⚠️ MEDIUM — app_setting.json 死字段被运行中进程写回
- 现象：我删 `sim_trader.live_signal_mode` 后，该字段又出现在 app_setting.json:202
- 根因：**后端服务仍在运行旧代码**，旧进程持有 `sc.LIVE_SIGNAL_MODE`，某次 `settings.save()` 把它写回
- 影响：死字段（代码已不读），功能无害，但配置文件不干净
- **归因**：不是本次代码 bug，是"删除后未重启服务"的预期表现。重启后端后旧进程退出，字段不再被写回

---

## ⚙️ 功能分析师发现

### ✅ 灰点变色逻辑正确
- loadSimMonitor（main.js:679-680）：`r.monitor_enabled ? 'var(--green)' : 'var(--text3)'`
- saveSimMonitor（main.js:704-705）：`body.monitor_enabled ? ...`（保存后同步）
- 两处一致，颜色用设计 token（var(--green)/var(--text3)），无硬编码

### ✅ LIVE 灯文字映射正确
- live_trader.js:29：`d.mode === 'live' ? '实盘·真钱' : (d.mode === 'dry-run' ? '模拟·不下单' : (d.mode || '—'))`
- 三分支覆盖：live / dry-run / 其他未知值，颜色映射保留（live→danger红，其他→warn橙）
- badge 自带圆点 dot，显示效果"● 实盘·真钱"

### ✅ saveSimSwitches body 删字段后结构完整
- body 现为 `{auto_sell, auto_scan, auto_buy}`（main.js:740-744），逗号/闭合正确
- 后端 `/api/settings/sim-switches` 处理 auto_*/monitor_*/strategy_name，前后端契约一致

### 🟡 LOW — 灰点初始闪烁
- HTML 默认 `background:#555`（index.html:1295），loadSimMonitor 是 async（切 tab 时调用）
- 若 monitor_enabled=true，用户切到 sim_trader tab 时灰点会从 #555 毫秒级闪到 green
- 影响：极轻微，正常使用不可察

---

## 🧪 测试师发现

### ✅ 现有测试全过
- 实跑 `pytest tests/test_sim_trader_store.py tests/test_buy_signal_bridge.py` → **47 passed, 1 warning in 2.63s**
- 1 个 warning 是预存在的 `DeprecationWarning: no current event loop`（websocket/manager.py:77），与本次改动无关

### ✅ 无测试引用已删代码
- 全 tests/ grep `LIVE_SIGNAL|live_signal|_forward|cron_jobs|pipeline_scheduler|_cleanup_signal|_state_lock` → 仅 `test_sim_trader_store.py:19 from app.sim_trader.config import INITIAL_CAPITAL`（引用 INITIAL_CAPITAL，非已删符号）
- `test_buy_signal_bridge.py` 测的是 live_trader **接收端**（schemas/闸门/心跳/鉴权），不涉及 sim_trader→live_trader 转发链路

### ⚠️ MEDIUM（预存在局限）— 本次改动未加新测试
- 灰点变色、LIVE 灯文字映射、删除回归均无单测
- 但项目前端无测试框架（tests/ 全是 Python 后端测试），这是**预存在局限**，非本次引入
- 建议（可选）：后端可加 `test_sim_switches_no_live_signal` 锁定 status 接口不再返回该字段

---

## 🖱️ 交互响应发现

### ✅ 删除后用户感知正确
- 模拟盘"执行开关"现在只剩三个：卖出|选股|买入，干净无乱码
- LIVE 灯文字清楚，红色配合"实盘·真钱"文案，不再误读为故障
- 灰点变色提供运行状态指示

### ✅ 缓存版本已 bump
- main.js?v=42、live_trader.js?v=6 —— 浏览器请求时自动拉新版（?v= 变化触发新请求，连普通刷新都生效，不强刷也行）

---

## 🤔 我额外想到的隐患

### 1. ⚠️ MEDIUM — 后端服务未重启的"半删除"状态
- 旧 cron 进程仍跑旧代码（含 `_forward_signals_to_live_trader`），daily 任务仍调 `self._cleanup_signal_state()`
- 默认 `LIVE_SIGNAL_MODE=off`，不会真转发，但旧逻辑"名义上还在"
- **若部分重启**（如重启 API 没重启 cron worker）：API 新代码不认 live_signal_mode，cron 旧代码还在——所幸默认 off 不触发，低风险
- **解决**：重启全部后端服务

### 2. 🟡 LOW — 运行时残留文件
- `data/live_trader/signal_state.json`：删代码后不再写入，旧文件残留无害
- 建议：手动删（可选）

### 3. 🟡 LOW — cron_jobs.py 三连续空行
- cron_jobs.py:525-527 类结束后有三个空行（原类内空行 + 模块空行叠加）
- PEP8 建议顶级定义间 2 空行，3 空行是风格小瑕疵，不影响运行

### 4. 🟡 LOW — docs/ 历史文档未清理
- 多篇 docs（实盘选股买入桥接系列、审计报告系列）仍大量提到"实盘信号转发"
- 作为历史记录保留合理，但后人可能困惑"文档说的功能代码里没有"
- 建议：保留（历史可追溯），不强制改

### 5. ✅ CLAUDE.md 一致性良好
- CLAUDE.md 无任何"实盘信号转发/live_signal/信号转发"引用，删功能不需改文档

---

## 📊 总评

| 级别 | 数量 | 说明 |
|---|---|---|
| **CRITICAL** | 0 | 无安全/数据/资金/核心功能问题 |
| **HIGH** | 0 | 无逻辑 bug / 无破坏现有功能 |
| **MEDIUM** | 2 | ① app_setting 死字段（重启解决）② 未加新测试（预存在局限） |
| **LOW** | 4 | 灰点闪烁 / 残留文件 / 三空行 / 历史文档 |

- **整体评分**: **9/10**
- **是否可交付**: **是**（无阻塞项）
- **核心质量**: 删除干净（零残留）、结构完整（py_compile + 47 测试通过）、前后端契约一致、向后兼容、可回滚
- **扣分项**: MEDIUM 都是"运行环境状态"（需重启）或"预存在局限"（项目无前端测试），非本次代码缺陷

---

## 🔧 建议修复项（按优先级）

| 优先级 | 项 | 动作 | 归因 |
|---|---|---|---|
| P1 | 重启后端服务 | 彻底清除旧进程 + app_setting 死字段 | 运行环境（用户操作） |
| P2（可选）| 手动删 `data/live_trader/signal_state.json` | 清运行时残留 | 运维清理 |
| P3（可选）| cron_jobs.py:525-527 三空行改两空行 | 风格 | LOW |
| P4（可选）| 加 `test_sim_switches_no_live_signal` 回归测试 | 防字段复活 | 预存在局限 |

---

## 验证证据汇总

| 验证项 | 命令/位置 | 结果 |
|---|---|---|
| Python 语法 | `py_compile` config/sim_trader/cron_jobs | OK |
| JS 语法 | `node -c` main.js/live_trader.js | OK |
| JSON 合法 | `json.tool` app_setting.json | OK |
| 代码残留 | 全项目 grep（.py/.js/.html/.json）| 零残留 |
| 现有测试 | pytest test_sim_trader_store + test_buy_signal_bridge | 47 passed |
| DOM 闭合 | index.html:1319/1324（display/edit div 各自闭合）| OK |
| 结构完整 | cron_jobs daily / sim_trader status+手动触发 / config | OK |
| CLAUDE.md 一致 | grep 转发关键词 | 无引用 |
