# 实施报告：实盘手工交易下单功能

> 日期：2026-07-18 | 对应计划书：docs/superpowers/plans/2026-07-18-manual-order-feature.md (v1.1)
> 状态：**已完成，测试全绿，待重启 live_trader 生效**

## 一、任务完成对照（计划 T1-T12）

| # | 任务 | 状态 | 产出/证据 |
|---|---|---|---|
| T1 | price_type.py 市价映射 | ✅ | app/live_trader/price_type.py；7 键映射+市场感知降级+ETF 前缀(自查补充) |
| T2 | /live/order 增强 | ✅ | main.py：dry-run 硬拒 403 + price_type_key 映射 + 市价单 QMT 实时价估算 fail-closed + 限价 price=0 拒(自审计补充) |
| T3 | POST /live/order/cancel | ✅ | main.py；kill_switch 放行；audit 留痕 |
| T4 | POST /live/positions/sync | ✅ | main.py；_takeover_positions 加可选 code 参数(向后兼容) |
| T5 | 下单表单 UI | ✅ | index.html "手工下单"折叠区(买入/卖出 Tab+价格类型下拉+价格/数量+资金估算) |
| T6 | 表单逻辑 | ✅ | live_trader.js：6位代码校验/买量规则/二次确认市价风险提示/金额实时估算/**市价下拉按市场动态过滤** |
| T7 | 撤单按钮 | ✅ | live_trader.js 委托表加"操作"列(已报50/部成55 可撤) |
| T8 | 下单后自动持仓同步 | ✅ | 成功 5s 后调 /live/positions/sync + 刷新持仓 |
| T9 | 缓存 bump | ✅ | live_trader.js v19→v20 |
| T10 | pytest 全套 | ✅ | **564 passed**, 2 failed(均 pre-existing,见下) |
| T11 | py_compile + node -c | ✅ | 全过 |
| T12 | 自审计+报告 | ✅ | 本文件 |

## 二、自审计（CLAUDE.md 4 维度）

| 维度 | 结果 |
|---|---|
| 死代码 | ⚠️ 1 项已知：price_type.py 的 `needs_price/key_name/available_keys` 目前仅测试使用（生产端点只用 `map_price_type`）；前端 JS 复制了同样的过滤规则（MO_PRICE_TYPES），两边已互相注释"修改要同步"。保留原因：构成模块公开 API 且被测试锁定；后续可加 GET /live/price-types 端点收敛为单一真相源 |
| 硬编码值 | ✅ 前端颜色全部 var(--red/--green/--text2)；无 hex；禁用态用 opacity 非颜色 |
| 线程/锁安全 | ✅ price_type 纯函数无状态；端点只读 _state 无锁；store 写入走既有 _db_lock；QMT 调用不持锁 |
| 边界条件 | ✅ 空 key 向后兼容；未知 key 400；市价单无行情 503 fail-closed；限价 price=0 400(自审计发现已修)；撤单 order_id 缺失/非法 400、QMT 异常 500；sync 时 QMT 未连接 503；前端非实盘 tab 元素缺失有 guard |

**自审计发现并已修的问题（1 个）**：限价单 price=0 后端未拦截（前端有校验但后端没有），会带 0 元限价进 QMT → 已加 400 校验 + 测试锁定。

## 三、测试结果

- **全套 564 passed**（新增 22 个：test_price_type 7 + test_manual_order 15）
- 2 failed = `test_critical_fixes.py` 的 `filled_price` 未定义（callback_handler.py:463），**pre-existing，与本功能无关**（上一轮已 stash 验证）
- py_compile / node -c 全过

## 四、行为变更声明（计划书 R2 落地）

- `/live/order` dry-run 从 mock 回报改为**硬拒 403**（用户确认）；buy-signal 链路不受影响（独立端点，executor 层 mock 不动）
- 已核实 tests/ 无任何测试直接打 /live/order，无回归

## 五、交付清单

| 文件 | 改动 |
|---|---|
| app/live_trader/price_type.py | 新建：市价映射+市场检测+降级 |
| app/live_trader/main.py | /live/order 增强；+POST /live/order/cancel；+POST /live/positions/sync；_takeover_positions 加 code 参数 |
| app/live_trader/schemas.py | OrderRequest 加 price_type_key |
| static/index.html | 手工下单折叠区；委托表加操作列；缓存 v20 |
| static/js/live_trader.js | 表单逻辑+撤单按钮+自动同步+dry-run 禁用态 |
| tests/test_price_type.py | 新建 7 个测试 |
| tests/test_manual_order.py | 新建 15 个测试 |

## 六、待办（用户侧）

1. **重启 live_trader** 生效（QMT 凭据需先确认可用）
2. 实盘验证：live 模式下择机手工下一笔小额限价单，走通"表单→10 闸门→QMT→委托列表→撤单/持仓同步"全链路
3. dry-run 下打开实盘 tab，确认"手工下单"折叠区整体禁用置灰
