# ============================================================
# P9 量化平台 - Docker/服务端依赖
# 适用环境: Linux Docker 容器 (api-server / task-worker)
# 注意: Windows GUI 专用包已移至 requirements-client.txt
# 最后更新: 2026-04-09
# ============================================================

# --- 核心数据引擎 ---
duckdb>=0.10.0
pandas>=2.1.0
numpy>=1.26.0
pyarrow>=14.0.0
fastparquet>=2023.10.0
polars>=0.20.0
vectorbt>=0.26.0
# TA-Lib C库在 Dockerfile 中通过 wheels 或源码编译安装

# --- 数据源（主: QMT代理 / 备: Tushare/Baostock/TDX）---
tushare>=1.4.0
baostock
pytdx2
akshare

# --- Web 框架 & API ---
fastapi>=0.100.0
uvicorn>=0.20.0
jinja2>=3.0.0
python-multipart>=0.0.6
websockets>=12.0
python-socketio>=5.11.2
aiohttp>=3.9.0

# --- 任务调度 ---
apscheduler>=3.10.0

# --- 工具库 ---
redis>=5.0.0
hiredis>=2.0.0
loguru>=0.7.0
pydantic>=2.0.0
pydantic-core
PyYAML>=6.0
tqdm>=4.65.0
requests>=2.28.0
python-dotenv>=1.0.0
python-dateutil
feedparser>=6.0.10
optuna>=3.5.0

# --- AI / LLM ---
openai>=1.0.0
langgraph
langchain-core
langchain-openai

# [已移除] vnpy        - Windows GUI 框架，已移至 requirements-client.txt
# [已移除] easytrader  - Windows 鼠标控制，已由 QMT 代理替代
# [已移除] pywinauto   - Windows UI 自动化，Docker 无法运行
# [已移除] PySide6     - Qt GUI，Docker 无法运行
# [已移除] pyqtgraph   - Qt 图表，Docker 无法运行
# [已移除] ta-lib-easy - 非标准包，TA-Lib 已在 Dockerfile 中单独处理