services:
  # 你的核心代码：用本地修改的源码构建
  quant-platform:
    build: ./quant-platform
    ports:
      - "8000:8000"
    depends_on:
      - redis
    restart: always

  # [2026-07-11 已停用] trendradar 源码目录已清理，本地镜像 p9-trendradar:latest 不存在
  # trendradar:
  #   image: p9-trendradar:latest
  #   restart: always

  # [2026-07-11 已停用] trendradar-mcp 同上
  # trendradar-mcp:
  #   image: p9-trendradar-mcp:latest
  #   restart: always

  # 数据库工具，不动
  redis:
    image: redis:7-alpine
    restart: always